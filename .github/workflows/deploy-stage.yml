name: Deploy to Stage

on:
  push:
    branches:
      - stage
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Load environment variables
        run: |
          echo "Loading stage environment variables..."
          cp .env.stage .env
          
      - name: Run database migrations
        run: npm run db:migrate
        env:
          DATABASE_URL: ${{ secrets.STAGE_DATABASE_URL }}

      - name: Build application
        run: npm run build
        env:
          NODE_ENV: stage

      - name: Run tests
        run: npm test --if-present

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.STAGE_AZUREAPPSERVICE_CLIENTID }}
          tenant-id: ${{ secrets.STAGE_AZUREAPPSERVICE_TENANTID }}
          subscription-id: ${{ secrets.STAGE_AZUREAPPSERVICE_SUBSCRIPTIONID }}

      - name: Deploy to Azure Stage
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'mohaweaves-stage'
          slot-name: 'Production'
          package: .
